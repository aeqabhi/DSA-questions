class Reverse {
    class Node{
        int data;
        Node next;
        Node prev;

        Node(int data){
            this.data = data;
            this.next= null;
            this.prev = null;
        }

        static void printll(Node head){
            Node temp = head;
        }
    } 
}
